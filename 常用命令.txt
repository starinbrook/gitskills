
#查看本地仓库和关联的远程仓库 状态
 git status

#提交本地修改到远程仓库（本地有修改）
  
1、git clone git@github.com:starinbrook/gitskills.git
2、git add
3、git commit
4、git push


#获取远程修改到本地（远程有修改）

1、git fetch 只会将本地库所关联的远程库的commit id更新至最新，不更新内容
2、git status 
3、git pull  会将本地库更新至远程库的最新状态
4、git status
